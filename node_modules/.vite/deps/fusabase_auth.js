import {
  App,
  Auth,
  AuthCredential,
  AuthError,
  AuthProvider,
  Config,
  EmailAuthCredential,
  EmailAuthProvider,
  ErrorCodeMessage,
  FacebookAuthProvider,
  GithubAuthProvider,
  GoogleAuthProvider,
  IDCSAuthProvider,
  IDCSConfig,
  IdTokenResult,
  OAuthCredential,
  OAuthProvider,
  ONPREMConfig,
  SAMLAuthCredential,
  SAMLAuthProvider,
  User,
  UserCredential,
  argCheck,
  authErrorHandler,
  browserLocalPersistence,
  browserSessionPersistence,
  fusabase_internal_default,
  inMemoryPersistence,
  typeStrings
} from "./chunk-FK3VHIU5.js";
import "./chunk-BG5AI4QA.js";

// node_modules/fusabase/dist/auth/src/internal/core.js
function getAuth(app) {
  app = app == null ? fusabase_internal_default.app() : app;
  if (!(app instanceof App)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid app instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return fusabase_internal_default.auth(app);
}
function initializeAuth(app, deps) {
  if (!(app instanceof App)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid app instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  argCheck(deps, "Invalid settings object", false, [typeStrings.OBJECT]);
  var pers_arr = [];
  if (deps == null ? void 0 : deps.persistence) {
    if (Array.isArray(deps.persistence)) {
      for (let i = 0; i < deps.persistence.length; i++) {
        pers_arr.push(deps.persistence[i].type);
      }
    } else {
      pers_arr.push(deps.persistence.type);
    }
  }
  var available_pers = ["LOCAL", "SESSION", "NONE"];
  for (let i = 0; i < 3; i++) {
    if (!pers_arr.includes(available_pers[i])) {
      pers_arr.push(available_pers[i]);
    }
  }
  return fusabase_internal_default.auth(app);
}
function beforeAuthStateChanged(auth, callback, onAbort) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return auth.beforeAuthStateChanged(callback, onAbort);
}
function setPersistence(auth, persistence) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return auth.setPersistence(persistence);
}

// node_modules/fusabase/dist/auth/src/internal/token.js
function onIdTokenChanged(auth, nextOrObserver, error, completed) {
  if (!(auth instanceof Auth)) {
    let error2 = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error2.status = 400;
    throw authErrorHandler(error2);
  }
  return auth.onIdTokenChanged(nextOrObserver);
}

// node_modules/fusabase/dist/auth/src/public/common.js
function onAuthStateChanged(auth, nextOrObserver, error, completed) {
  if (!(auth instanceof Auth)) {
    let error2 = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error2.status = 400;
    throw authErrorHandler(error2);
  }
  return auth.onAuthStateChanged(nextOrObserver);
}

// node_modules/fusabase/dist/auth/src/public/email.js
function createUserWithEmailAndPassword(auth, email, password) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return auth.createUserWithEmailAndPassword(email, password);
}

// node_modules/fusabase/dist/auth/src/public/password.js
function sendPasswordResetEmail(auth, email, actionCodeSettings) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return auth.sendPasswordResetEmail(email, actionCodeSettings);
}
function verifyPasswordResetCode(auth, code) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return auth.verifyPasswordResetCode(code);
}
function confirmPasswordReset(auth, code, newPassword) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return auth.confirmPasswordReset(code, newPassword);
}

// node_modules/fusabase/dist/auth/src/public/strategies.js
function signInWithCredential(auth, credential) {
  return auth.signInWithCredential(credential);
}
function signInWithPopup(auth, provider, resolver) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return auth.signInWithPopup(provider);
}
function signInWithRedirect(auth, provider, resolver) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return auth.signInWithRedirect(provider);
}
function signInWithEmailAndPassword(auth, email, password) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return auth.signInWithEmailAndPassword(email, password);
}
async function updateCurrentUser(auth, user) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  await auth.updateCurrentUser(user);
}
function signOut(auth) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return auth.signOut();
}

// node_modules/fusabase/dist/auth/src/public/token.js
function getRedirectResult(auth, resolver) {
  if (!(auth instanceof Auth)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid authentication instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return auth.getRedirectResult();
}

// node_modules/fusabase/dist/auth/src/public/user.js
async function getIdToken(user, forceRefresh) {
  if (!(user instanceof User)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid user instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  let token = await user.getIdToken(forceRefresh);
  return token ?? "";
}
function getIdTokenResult(user, forceRefresh) {
  if (!(user instanceof User)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid user instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return user.getIdTokenResult(forceRefresh);
}
function reload(user) {
  if (!(user instanceof User)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid user instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return user.reload();
}
function sendEmailVerification(user, actionCodeSettings) {
  if (!(user instanceof User)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid user instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return user.sendEmailVerification(actionCodeSettings);
}
function updatePassword(user, oldPassword, newPassword) {
  if (!(user instanceof User)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid user instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return user.updatePassword(oldPassword, newPassword);
}
function updateProfile(user, profile) {
  if (!(user instanceof User)) {
    let error = new AuthError(ErrorCodeMessage.INVALID_ARGS, "Invalid user instance");
    error.status = 400;
    throw authErrorHandler(error);
  }
  return user.updateProfile(profile);
}
function linkWithCredential(user, credential) {
  return user.linkWithCredential(credential);
}
function linkWithRedirect(user, provider, resolver) {
  return user.linkWithRedirect(provider);
}
function linkWithPopup(user, provider, resolver) {
  return user.linkWithPopup(provider);
}
function unlink(user, providerId) {
  return user.unlink(providerId);
}
export {
  Auth,
  AuthCredential,
  AuthError,
  AuthProvider,
  Config,
  EmailAuthCredential,
  EmailAuthProvider,
  FacebookAuthProvider,
  GithubAuthProvider,
  GoogleAuthProvider,
  IDCSAuthProvider,
  IDCSConfig,
  IdTokenResult,
  OAuthCredential,
  OAuthProvider,
  ONPREMConfig,
  SAMLAuthCredential,
  SAMLAuthProvider,
  User,
  UserCredential,
  beforeAuthStateChanged,
  browserLocalPersistence,
  browserSessionPersistence,
  confirmPasswordReset,
  createUserWithEmailAndPassword,
  getAuth,
  getIdToken,
  getIdTokenResult,
  getRedirectResult,
  inMemoryPersistence,
  initializeAuth,
  linkWithCredential,
  linkWithPopup,
  linkWithRedirect,
  onAuthStateChanged,
  onIdTokenChanged,
  reload,
  sendEmailVerification,
  sendPasswordResetEmail,
  setPersistence,
  signInWithCredential,
  signInWithEmailAndPassword,
  signInWithPopup,
  signInWithRedirect,
  signOut,
  unlink,
  updateCurrentUser,
  updatePassword,
  updateProfile,
  verifyPasswordResetCode
};
//# sourceMappingURL=fusabase_auth.js.map
