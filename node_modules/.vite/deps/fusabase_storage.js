import {
  App,
  ListResult,
  Storage,
  StorageError,
  StorageErrorCode,
  StorageReference,
  TaskEvent,
  TaskState,
  UploadTask,
  UploadTaskSnapshot,
  fusabase_internal_default,
  getStorageErrorMessage,
  storageErrorHandler
} from "./chunk-FK3VHIU5.js";
import "./chunk-BG5AI4QA.js";

// node_modules/fusabase/dist/storage/src/list-result.js
async function list(ref2, options) {
  if (!(ref2 instanceof StorageReference)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_REFERENCE"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  return ref2.list(options);
}
async function listAll(ref2) {
  if (!(ref2 instanceof StorageReference)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_REFERENCE"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  return ref2.listAll();
}

// node_modules/fusabase/dist/storage/src/metadata.js
async function getMetadata(ref2) {
  if (!(ref2 instanceof StorageReference)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_REFERENCE"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  return ref2.getMetadata();
}

// node_modules/fusabase/dist/storage/src/reference.js
function throwInvalidDownloadSize() {
  const error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_DOWNLOAD_SIZE"));
  error.status = 400;
  throw storageErrorHandler(error);
}
function throwDownloadLimitExceeded(maxBytes) {
  const error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("DOWNLOAD_EXCEEDS_LIMIT", String(maxBytes)));
  error.status = 400;
  throw storageErrorHandler(error);
}
async function assertMetadataWithinDownloadLimit(ref2, maxDownloadSizeBytes) {
  if (maxDownloadSizeBytes == null)
    return;
  if (!Number.isSafeInteger(maxDownloadSizeBytes) || maxDownloadSizeBytes <= 0) {
    throwInvalidDownloadSize();
  }
  const metadata = await ref2.getMetadata();
  if (metadata.size > maxDownloadSizeBytes) {
    throwDownloadLimitExceeded(maxDownloadSizeBytes);
  }
}
function ref(storage, path) {
  if (!(storage instanceof Storage || storage instanceof StorageReference)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_STORAGE"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  if (path != null && (path.startsWith("http://") || path.startsWith("https://"))) {
    if (storage instanceof StorageReference) {
      let err = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_URL_REFERENCE"));
      err.status = 400;
      throw storageErrorHandler(err);
    }
    return storage.refFromURL(path);
  }
  if (storage instanceof StorageReference) {
    return storage.child(path);
  }
  return storage.ref(path);
}
async function getBytes(ref2, maxDownloadSizeBytes) {
  if (!(ref2 instanceof StorageReference)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_REFERENCE"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  await assertMetadataWithinDownloadLimit(ref2, maxDownloadSizeBytes);
  const res = await ref2.downloadData(maxDownloadSizeBytes);
  if (res) {
    return res.arrayBuffer();
  } else {
    throw new StorageError(StorageErrorCode.UNKNOWN, getStorageErrorMessage("UNKNOWN"));
  }
}
async function getBlob(ref2, maxDownloadSizeBytes) {
  if (!(ref2 instanceof StorageReference)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_REFERENCE"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  await assertMetadataWithinDownloadLimit(ref2, maxDownloadSizeBytes);
  const res = await ref2.downloadData(maxDownloadSizeBytes);
  if (res) {
    return res.blob();
  } else {
    throw new StorageError(StorageErrorCode.UNKNOWN, getStorageErrorMessage("UNKNOWN"));
  }
}
async function getStream(ref2, maxDownloadSizeBytes) {
  if (!(ref2 instanceof StorageReference)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_REFERENCE"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  await assertMetadataWithinDownloadLimit(ref2, maxDownloadSizeBytes);
  return ref2.downloadData(maxDownloadSizeBytes);
}
async function deleteObject(ref2) {
  if (!(ref2 instanceof StorageReference)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_REFERENCE"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  return ref2.delete();
}
async function getDownloadURL(ref2) {
  if (!(ref2 instanceof StorageReference)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_REFERENCE"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  return ref2.getDownloadURL();
}

// node_modules/fusabase/dist/storage/src/storage.js
function getStorage(app, url) {
  app = app == null ? fusabase_internal_default.app() : app;
  if (!(app instanceof App)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_APP"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  return fusabase_internal_default.storage(app);
}

// node_modules/fusabase/dist/storage/src/upload-task.js
async function uploadBytes(ref2, data, metadata) {
  if (!(ref2 instanceof StorageReference)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_REFERENCE"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  const task = ref2.put(data, metadata);
  const snapshot = await task;
  return { ref: ref2, metadata: snapshot.metadata };
}
function uploadBytesResumable(ref2, data, metadata) {
  if (!(ref2 instanceof StorageReference)) {
    let error = new StorageError(StorageErrorCode.INVALID_ARGUMENT, getStorageErrorMessage("INVALID_REFERENCE"));
    error.status = 400;
    throw storageErrorHandler(error);
  }
  return ref2.put(data, metadata);
}
export {
  ListResult,
  Storage,
  StorageError,
  StorageReference,
  TaskEvent,
  TaskState,
  UploadTask,
  UploadTaskSnapshot,
  deleteObject,
  getBlob,
  getBytes,
  getDownloadURL,
  getMetadata,
  getStorage,
  getStream,
  list,
  listAll,
  ref,
  uploadBytes,
  uploadBytesResumable
};
//# sourceMappingURL=fusabase_storage.js.map
